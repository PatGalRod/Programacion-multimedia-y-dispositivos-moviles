// Jose Patricio Galán Rodriguez

//Indica si un número es par o impar, usando la sintaxis de if como expresión,
// en vez la sintaxis clásica.

fun main() {
    val n1 = 24
    val resultado = if (n1 % 2 == 0)"Este número es par" else "Este número no es par"

    println(resultado)
}
